spring.application.name=server
server.port=8080

# DATASOURCE
spring.datasource.url=${DB_URL:jdbc:postgresql://express-db.c9kggwm2mug3.us-east-2.rds.amazonaws.com:5432/ExpressDB}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:12345678}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.default_schema=public

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# SECURITY
app.security.jwt.secret=${JWT_SECRET:AReaLlyLongAndCompLexJwtSecrEtForLoCalDevElopMentOnLy12345}